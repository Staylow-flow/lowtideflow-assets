{
  "name": "lowtideflow-apparel",
  "version": "1.0.0",
  "private": true,
  "description": "Lowtideflow Apparel — local dev server for hero preview",
  "scripts": {
    "start": "npx serve . --listen 3000 --single",
    "dev":   "npx serve . --listen 3000"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
