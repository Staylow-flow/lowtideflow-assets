/**
 * @deprecated Layout injection removed — use Webflow Designer + page head FX only.
 * Kept as empty stub so old import paths do not break during footer pin transitions.
 */
export function init() {}

export default init;
